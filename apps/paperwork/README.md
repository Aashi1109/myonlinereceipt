# SmartTools Paperwork

The SmartTools project for invoices, receipts, expenses, mileage, tax estimates, W-9 requests, and 1099 tracking.

From the repository root:

```bash
pnpm dev:paperwork
```

Copy `.env.example` to `.env.local` and configure only the server features you use.
