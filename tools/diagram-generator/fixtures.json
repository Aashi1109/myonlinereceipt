{
  "cases": [
    {
      "expected": {
        "error": "Mermaid diagram rendering requires a browser."
      },
      "input": {
        "primary": "flowchart LR\n  A[Input] --> B[Transform] --> C[Output]",
        "secondary": ""
      },
      "name": "no DOM available",
      "settings": {}
    },
    {
      "expected": {
        "error": "Direction overrides are only supported for Mermaid flowcharts."
      },
      "input": {
        "primary": "sequenceDiagram\n  Client->>API: Request",
        "secondary": ""
      },
      "name": "rejects a direction override for non-flowcharts",
      "settings": {
        "direction": "LR"
      }
    }
  ]
}
